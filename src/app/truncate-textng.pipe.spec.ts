import { TruncateTextngPipe } from './truncate-textng.pipe';

describe('TruncateTextngPipe', () => {
  it('create an instance', () => {
    const pipe = new TruncateTextngPipe();
    expect(pipe).toBeTruthy();
  });
});
